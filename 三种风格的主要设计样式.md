---
title: 绿色低碳产业生态智慧链接平台-三种设计风格
---


为了方便您的前端开发团队快速落地，实现“一套业务逻辑，多套UI一键换肤”**的设计目标，以下为您提供直接面向项目编程使用的**全套技术交付物。

本规范完全基于您技术方案中的 **React 18 + Tailwind CSS 3.x** 技术栈，通过原子化 CSS 的变量映射（Theme Tokens）进行设计。开发人员只需完成以下三步配置与组件编写，即可直接在项目中使用。

## 一、 核心设计令牌（Tailwind Config 配置）

前端团队需要首先修改项目根目录下的 `tailwind.config.js`，将三种风格的色彩矩阵规范化为**语义化扩展颜色**。这样在编写组件时，可以通过直接调用自定义类名（如 `bg-brand-tech-surface`）实现完美的样式隔离。

JavaScript

```
// tailwind.config.js
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          // ================= 风格 A: 智慧极客绿 (Tech-Sleek Eco) =================
          tech: {
            bg: '#020617',         // 深邃蓝黑底色 (slate-950)
            surface: '#0f172a',    // 组件/卡片背板色 (slate-900)
            neon: '#34d399',       // 核心高亮荧光绿 (emerald-400)
            cyan: '#22d3ee',       // 科技感青色高光 (cyan-400)
            border: '#1e293b',     // 弱化暗色边框 (slate-800)
          },
          // ================= 风格 B: 清新北欧绿 (Clean Nordic) =================
          nordic: {
            bg: '#fafaf9',         // 柔和高质感奶白底色 (stone-50)
            surface: '#ffffff',    // 纯白卡片背景
            forest: '#047857',     // 茂林深绿主色调 (emerald-700)
            clay: '#78716c',       // 莫兰迪自然灰色 (stone-500)
            border: '#e7e5e4',     // 极轻量边框 (stone-200)
          },
          // ================= 风格 C: 商务蔚蓝绿 (Vibrant Blue-Green) =================
          office: {
            bg: '#f8fafc',         // 标准政企灰白底色 (slate-50)
            surface: '#ffffff',    // 纯白卡片背景
            navy: '#0369a1',       // 金融级深宝蓝 (sky-700)
            mint: '#059669',       // 产业长青绿 (emerald-600)
            border: '#e2e8f0',     // 通用灰边框 (slate-200)
          }
        }
      },
      // 严格匹配方案 3.1 响应式断点策略
      screens: {
        'sm': '640px',   // 大屏手机 / 小平板竖屏
        'md': '768px',   // 标配平板
        'lg': '1024px',  // 笔记本电脑终端
        'xl': '1280px',  // 桌面显示器/指挥大盘
      }
    }
  },
  plugins: [],
}
```

## 二、 风格样式映射表（Style Mapping Matrix）

在 React 工程中，为了避免在组件内部写大量的 `if-else` 或 `switch` 导致代码臃肿，推荐建立一个**中心化的样式字典（Style Dictionary）**。前端在渲染页面或组件时，只需传入当前激活的风格 ID（`tech` | `nordic` | `office`），即可动态提取对应的 Tailwind 原子类类名组合。

JavaScript

```
// src/styles/themeSchema.js

export const COMPONENT_THEMES = {
  // 风格 A：智慧极客绿
  tech: {
    wrapper: 'bg-brand-tech-bg text-slate-100 min-h-screen',
    card: 'bg-brand-tech-surface border border-brand-tech-border rounded-xl p-5 hover:border-brand-tech-neon/50 shadow-lg shadow-emerald-950/10 transition-all duration-300',
    textMain: 'text-slate-100 font-bold',
    textMuted: 'text-slate-400 text-xs',
    accentText: 'text-brand-tech-neon font-mono font-bold',
    badge: 'bg-emerald-950/40 text-brand-tech-neon border border-emerald-800/60 font-mono text-[11px] px-2 py-0.5 rounded',
    primaryBtn: 'bg-gradient-to-r from-brand-tech-neon to-brand-tech-cyan text-slate-950 font-black px-4 py-2 rounded-lg hover:brightness-110 active:scale-95 transition-all shadow-md shadow-emerald-500/20',
    secondaryBtn: 'border border-slate-700 bg-slate-900/50 hover:bg-slate-800 text-slate-200 px-3 py-1.5 rounded-lg text-xs',
    divider: 'border-brand-tech-border'
  },
  
  // 风格 B：清新北欧绿
  nordic: {
    wrapper: 'bg-brand-nordic-bg text-stone-800 min-h-screen',
    card: 'bg-brand-nordic-surface border border-brand-nordic-border rounded-lg p-5 hover:shadow-xl hover:border-brand-nordic-forest/40 transition-all duration-200',
    textMain: 'text-stone-800 font-semibold',
    textMuted: 'text-brand-nordic-clay text-xs',
    accentText: 'text-brand-nordic-forest font-bold',
    badge: 'bg-emerald-550/5 text-brand-nordic-forest border border-emerald-200 px-2 py-0.5 text-[11px] font-semibold rounded',
    primaryBtn: 'bg-brand-nordic-forest hover:bg-emerald-800 text-white font-medium px-4 py-2 rounded shadow-sm active:scale-98 transition-all',
    secondaryBtn: 'border border-brand-nordic-border bg-white hover:bg-stone-50 text-stone-700 px-3 py-1.5 rounded text-xs',
    divider: 'border-brand-nordic-border'
  },
  
  // 风格 C：商务蔚蓝绿
  office: {
    wrapper: 'bg-brand-office-bg text-slate-800 min-h-screen',
    card: 'bg-brand-office-surface border border-brand-office-border rounded-xl p-5 hover:shadow-lg hover:border-cyan-500/50 transition-all duration-200',
    textMain: 'text-slate-800 font-bold',
    textMuted: 'text-slate-500 text-xs',
    accentText: 'text-brand-office-navy font-bold font-mono',
    badge: 'bg-cyan-50 text-cyan-700 border border-cyan-200/50 text-[11px] px-2 py-0.5 rounded',
    primaryBtn: 'bg-gradient-to-r from-brand-office-navy to-brand-office-mint hover:opacity-95 text-white font-medium px-4 py-2 rounded-lg active:scale-98 transition-all shadow-sm',
    secondaryBtn: 'border border-brand-office-border bg-white hover:bg-slate-50 text-slate-700 px-3 py-1.5 rounded-lg text-xs',
    divider: 'border-brand-office-border'
  }
};
```

## 三、 响应式核心业务组件标准落地代码

根据技术方案 3.2 章节的要求，以下提供平台一期的核心组件——**“自适应供需对接网格卡片（SupplyDemandGrid）”**。此组件内部代码**完全未包含任何死色和具体样式布局**，而是通过 Mobile First 响应式断点（`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`）以及上述的 Theme Mapping 联动。

工程师可以直接将其粘贴至项目的 `/src/components/` 目录下进行编译调用：

JavaScript

```
// src/components/SupplyDemandResponsiveGrid.jsx
import React from 'react';
import { COMPONENT_THEMES } from '../styles/themeSchema';

/**
 * 绿产智链 - 响应式多风格供需卡片网格组件
 * @param {Array} items - 供需数据池 (包含企业名称、低碳资质、信用分、标题等要素)
 * @param {string} currentTheme - 指定当前激活的 UI 风格 ('tech' | 'nordic' | 'office')
 * @param {function} onActionClick - 对接动作触发后的业务流回调
 */
export function SupplyDemandResponsiveGrid({ items = [], currentTheme = 'tech', onActionClick }) {
  
  // 安全获取样式类，如传入错误 key 则降级至极客绿风格
  const style = COMPONENT_THEMES[currentTheme] || COMPONENT_THEMES['tech'];

  return (
    <div className="w-full">
      {/* 技术方案3.1落地：Mobile First（移动优先）自适应响应式断点编排
        - 默认宽度 (<768px): 1列满宽堆叠，适配手机 H5 / 微信服务号内嵌页
        - md断点 (≥768px): 2列网格，适配 iPad/平板端
        - lg断点 (≥1024px): 3列布局，适配笔记本电脑
        - xl断点 (≥1280px): 4列延展看板，适配桌面宽屏显示器
      */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6">
        {items.map((data) => (
          <div 
            key={data.id} 
            className={`flex flex-col justify-between ${style.card}`}
          >
            {/* 卡片头部：分类与信用度要素 */}
            <div>
              <div className="flex justify-between items-center gap-2 mb-3">
                <span className={style.badge}>
                  {data.category || '双碳资产'}
                </span>
                {data.creditScore && (
                  <span className={style.textMuted}>
                    信用分: <span className={style.accentText}>{data.creditScore}</span>
                  </span>
                )}
              </div>

              {/* 业务核心标题 (限制双行，超出部分打点) */}
              <h3 className={`text-sm leading-snug tracking-wide line-clamp-2 mb-2 ${style.textMain}`}>
                {data.title}
              </h3>
              
              {/* 会员单位主体 */}
              <p className={`mb-3 truncate ${style.textMuted}`}>
                🏢 {data.company}
              </p>
              
              {/* 核心特色：绿色低碳资质认证标识 (技术方案4.2特色块扩展) */}
              {data.badges && data.badges.length > 0 && (
                <div className="flex flex-wrap gap-1 mb-4">
                  {data.badges.map((badgeText) => (
                    <span 
                      key={badgeText} 
                      className="text-[9px] bg-emerald-500/5 text-emerald-600 border border-emerald-500/15 px-1.5 py-0.5 rounded font-medium flex items-center gap-0.5"
                    >
                      🌱 {badgeText}
                    </span>
                  ))}
                </div>
              )}
            </div>
            
            {/* 卡片尾部：数据要素沉淀与对接动作触发 */}
            <div className={`pt-3.5 mt-2 border-t flex items-center justify-between ${style.divider}`}>
              <div>
                <span className={`block scale-90 origin-left opacity-75 ${style.textMuted}`}>
                  要素规模/意向金额
                </span>
                <span className={`text-xs font-bold font-mono ${style.accentText}`}>
                  {data.value || '结构化面议'}
                </span>
              </div>
              
              <button 
                onClick={() => onActionClick && onActionClick(data)}
                className={`text-xs py-1.5 px-3.5 font-semibold transition-all ${style.primaryBtn}`}
              >
                对接申请
              </button>
            </div>

          </div>
        ))}
      </div>
    </div>
  );
}
```

## 四、 混合部署及开发落地方案建议

为了最大化体现技术方案中一期“基础搭台”和二期“生态运营”的战略升级，建议在具体前端路由（Router）开发时采用“多换肤、专场专用”的混合开发策略：

1. **默认底座（全站通用）➔ `nordic`（清新北欧绿）**
    
    - **原因**：表单、长文、专家列表等页面操作频繁，北欧绿的高白空间距与低饱和度色彩能够最大程度降低视觉疲劳度，符合协会公信力背书。
        
2. **产业大数据中心路由（`/dashboard/metrics`）➔ `tech`（智慧极客绿）**
    
    - **原因**：专门包裹在 ClickHouse 统计、供需景气度指数、以及政企协同脱敏数据报送面板中。在向省发改委、工信厅专班以及国企高管做管理驾驶舱汇报时，全屏切入极客暗色调，具备更强的数据冲击力和智能化科技质感。
        
3. **绿色金融与线上签约专区（`/services/contract`）➔ `office`（商务蔚蓝绿）**
    
    - **原因**：在法大大/契约锁电子印章调用、合同签署、以及向银行机构申请绿色信贷的业务流程中，切入宝蓝与长青绿的商务渐变色，能够强化金融安全感与企业级合同签署的仪式感。